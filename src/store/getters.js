export default {
    selectedItems(state) {
        return state.selectedItems
    },
    counterSelectedItems(state) {
        return state.selectedItems.length
    },
    course(state) {
        return state.course
    },
    goods(state) {
        return state.goods
    },
}